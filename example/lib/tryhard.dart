import 'package:flutter/material.dart';

class Tryhard extends StatefulWidget {
  const Tryhard({Key? key}) : super(key: key);

  @override
  _TryhardState createState() => _TryhardState();
}

class _TryhardState extends State<Tryhard> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('âsdadasd'),
      ),
      body: Container(
        child: const Text('asdasd'),
      ),
    );
  }
}
